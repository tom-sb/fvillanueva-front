import { Box, Typography } from '@mui/material';

export default function SocialRespView() {
  return (
    <Box sx={{ width: '100%' }}>
      <Typography>'SocialResp page'</Typography>
    </Box>
  );
}
